name: CI

on:
  push:
  pull_request:

jobs:
  backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install -e ".[dev]"
      - run: pytest -q
      - run: ruff check src tests scripts 2>&1 | tee backend-ruff.log
      - run: ruff format --check src tests scripts 2>&1 | tee backend-format.log
      - run: mypy src/portrait_eval 2>&1 | tee backend-mypy.log
      - name: Upload backend validation logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: backend-validation-logs
          path: backend-*.log

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - run: npm install --no-package-lock
      - run: npm test -- --run 2>&1 | tee frontend-test.log
      - run: npm run build 2>&1 | tee frontend-build.log
      - name: Upload frontend validation logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: frontend-validation-logs
          path: web/frontend-*.log
